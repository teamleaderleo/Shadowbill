{
  "name": "proofwake",
  "version": "0.3.0",
  "private": true,
  "description": "A local evidence index for software projects.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "proofwake": "./src/main.js",
    "shadowbill": "./src/main.js"
  },
  "scripts": {
    "test": "bash -o pipefail -c 'node --test test/*.test.js 2>&1 | tail -120'",
    "serve": "node src/cli.js serve",
    "report": "node src/cli.js report",
    "mcp": "node src/cli.js mcp",
    "emit": "node src/main.js emit"
  },
  "engines": {
    "node": ">=22"
  }
}
